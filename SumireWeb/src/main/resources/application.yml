spring:
  profiles:
    active: local
mybatis:
  config-location: classpath:/mybatis/mybatis-config.xml
  mapper-locations: classpath*:/mybatis/mapper/**/*.xml
server:
  port: ${PORT:5002}
logging:
  file: 
    name: var/log/sumire/sumire.web.log
  charset:
    file: UTF-8
    console: UTF-8
